@model IEnumerable<BeanSceneAppV1.Models.Table>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Table_Id)</th>
            <th>@Html.DisplayNameFor(m => m.Area)</th>
            <th>@Html.DisplayNameFor(model => model.Table_Seats)</th>    
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@Html.DisplayFor(modelItem => item.Table_Id)</td>
                <td>@Html.DisplayFor(modelItem => item.Area.Area_Name)</td>
                <td>@Html.DisplayFor(modelItem => item.Table_Seats)</td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Table_Id">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Table_Id">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Table_Id">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
